// -*- mode: javascript; -*- // @flow

import type { Disposable } from "./Disposable.js.flow";

declare class BufferedProcess {
  constructor(
    options: {
      command: string,
      args?: string[],
      options?: Object,
      stdout?: (data: string) => void,
      stderr?: (data: string) => void,
      exit?: (code: number) => void,
    }
  ): void;

  kill(): void;

  onWillThrowError(
    callback: (errorObject: { error: Object, handle(): void }) => void
  ): Disposable;
}

export type { BufferedProcess };
