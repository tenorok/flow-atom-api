// -*- mode: javascript; -*- // @flow
/* eslint-disable no-undef */

declare type FindMarkersParameters =
  | { startPosition?: Point
    , endPosition?: Point
    , containsPoint?: Point
    , containsRange?: Range
    , startRow?: number
    , endRow?: number
    , intersectsRow?: number
    }

declare type MarkRangeProperties =
  | { reversed: boolean
    , persistent: boolean
    , invalidate: "inside" | "never" | "overlap" | "surround" | "touch"
    }

declare class MarkerLayer {
  copy(): MarkerLayer;
  destroy(): void;
  findMarkers(params: FindMarkersParameters): TextEditorMarker[]; // FIXME
  getMarker(id: number): TextEditorMarker; // FIXME
  getMarkers(): TextEditorMarker[];
  getMarkerCount(): number;
  isDestroyed(): boolean; // FIXME
  markRange(range: Range, properties: MarkRangeProperties): TextEditorMarker; // FIXME
  markPosition(position: Point): TextEditorMarker;
}
