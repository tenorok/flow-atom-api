// -*- mode: javascript; -*- // @flow
/* eslint-disable no-undef */

declare class CompositeDisposable {
  constructor(...disposables: Disposable[]): this; // FIXME
  add(...disposables: Disposable[]): void;
  clear(): void;
  dispose(): void;
  remove(disposable: Disposable): void;
}
